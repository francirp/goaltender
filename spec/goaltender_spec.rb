require 'spec_helper'

describe Goaltender do
  it 'has a version number' do
    expect(Goaltender::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
